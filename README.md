# Anonymously — Frontend

This is the React + Vite frontend for **Anonymously**, a simple anonymous chat app with temporary, password-protected rooms.

## ✨ Features
- Create or join chat rooms
- Optional room passwords
- Real-time messaging (WebSockets)
- Image sharing (size-limited)
- Auto-expiring rooms
- Dark, modern UI (responsive)
- No logins or accounts required

## 🚀 Run Locally
```bash
cd frontend
npm install
npm run dev
